
using Microsoft.Data.Sqlite;
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace MixedLibrary.DataAccess
{
    public class SQLLiteConnector // : IDataConnection
    {/*
        public ProtocolModel CreateProtocol(ProtocolModel model)
        {
            
            using (IDbConnection dataConnection = new SqliteConnection(GlobalConfig.ConnectionValue("ProtocolsLite")))
            {
                // input all data from model, if output (from database) then specify
                var dapperVar = new DynamicParameters();
                dapperVar.Add("@id", 0, dbType: DbType.Int32);
                dapperVar.Add("@description", model.description, dbType: DbType.String);
                dapperVar.Add("date", DateTime.UtcNow, dbType: DbType.DateTime);

                return model;
            }
            
        }
*/
    }
    }
